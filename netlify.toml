[build]
  publish = "."
  command = ""

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
  # Do not force rewrite existing files (images/assets). For SPA fallback only.
  # force = true  # removed to allow static assets under /img/ to be served correctly
